document.addEventListener('DOMContentLoaded', function() {
    // console.log('Landing page JavaScript loaded.');
    // Future JavaScript for landing page interactivity (e.g., animations, form handling, etc.) can go here.

    // Example: Smooth scroll for internal links (already added inline in index.html, but could be moved here)
    // document.querySelectorAll('nav a[href^="#"]').forEach(anchor => {
    //     anchor.addEventListener('click', function (e) {
    //         e.preventDefault();
    //         const targetId = this.getAttribute('href');
    //         const targetElement = document.querySelector(targetId);
    //         if(targetElement) {
    //             targetElement.scrollIntoView({ behavior: 'smooth' });
    //         }
    //     });
    // });
});
